# 実装・検証状況

更新: 2026-09-12
基準: 要件2.3、システム設計1.3、Proxy API v2契約案0.2。

**生成画像の自動添付・モデル選択・状態案内の修正を実装し、ユーザー用systemdサービスへ反映済みです。利用者から実Discordでの正常動作の確認を得ました。** 以下は実装範囲と検証記録です。

## 実装済みの範囲

| 対象 | 内容 |
| --- | --- |
| 責務境界 | Gatewayにcwd登録を要求せず、Proxyが会話・ワーク・確定回答・成果物の正本を管理 |
| 会話と実行 | 独立会話作成、会話IDによる継続、元キーによる受理照会、最大1回送信、同一会話・workspaceの排他 |
| 制御 | 実行枠とは独立した承認・Steer・停止・監視、Turn ID未確定時の停止、停止受付と完了の区別 |
| 選択UI | モデル・共有ワーク選択、フォーラムからの新規会話、成果物のページ送り・期限付き選択、共有範囲の明示選択 |
| 回答と成果物 | 固定ID取得、サイズ・SHA-256検証、強いETagとRangeによる中断取得再開、有限リースの取得・延長・解放照会 |
| 配信復旧 | Discord送信意思の永続化と受領照合、DB再オープン後の確定回答再取得、重複リスク確認付き /retry。AIを再実行して補わない |
| エラー処理 | 容量・期限・権限・破損の区別、取得のbackoff、未受理が明示されたcapture容量拒否だけ同一キーで再試行 |
| 表示とtemp | 通常成功時は回答本文を中心に表示、確定回答の余剰投稿整理、所有確認付き孤立キャッシュ回収 |
| DB・復元 | schema 5、既知schemaのtransaction移行とchecksum確認、旧依頼隔離、Proxy世代変更時の照合票と監査付き明示受入 |
| UNKNOWN | 自動再実行禁止、Proxy監査解除の照合、旧会話隔離、遅着RUNNING時の占有再取得 |
| 資料 | 英日README・導入・ユーザーマニュアル・運用手順を更新 |

v1の実行開始・監視処理は削除しました。モデル一覧と制御に使う既存API、旧DBを安全に移行する互換構造は残しています。

## 実サービスで確認したこと

常駐ProxyはAPI v2の必須Capabilityを満たし、成果物登録ツールが有効でした。生成画像対応の最終確認では、Gatewayの運用設定が契約2.0であることも確認しました。

1. **実Proxy＋実Codex＋模擬Discord**：専用会話を自動作成し、実モデルがテキストファイルを作成、専用ツールで成果物登録、確定回答を保存。Gatewayが一覧から選択し、同じ成果物を取得・検証して配信しました。
2. **Gateway DB再オープン**：回答のメモリを失った状態から同じResponseの確定回答を取得して配信。新たなAI実行は行っていません。
3. **実Discord**：許可された投稿先へ「Gateway v2 配信テスト」と上記成果物を送信。Discordで投稿IDと添付サイズを確認しました。運用中Gatewayを置換せず、隔離した試験DBから同じ配信コードを使用しました。

これらは tests/live_proxy_v2.rs の明示実行専用テストです。通常のテストでは実サービスへ送信しません。

## 自動試験

HTTP応答喪失、同一キー照会、世代不一致、初回失敗後の継続、停止競合、リース失効・延長応答喪失、部分ダウンロード、明示再送の重複操作、移行隔離、選択の期限・会話・世代照合、復元の明示受入、余剰表示削除を検証します。

v2基本実装時の確認結果（生成画像対応の最新結果は末尾）:

- `cargo test --locked --tests --no-fail-fast --quiet`: 50件成功、失敗0。実サービス用3件は通常実行では除外し、それぞれ明示実行で成功を確認。
- `cargo clippy --locked --all-targets -- -D warnings`: 成功。
- `cargo fmt --all -- --check` と `git diff --check`: 成功。
- 英日資料のローカルリンク: 存在確認済み。
- `cargo build --locked --release`: 成功。生成バイナリの `--version` 起動を確認。

## 配備と追加の環境試験

公開資料は未リリース製品の初回導入向けです。開発中のDB互換処理は実装上の保護であり、製品のV1→V2移行手順として案内しません。導入は[導入手順](installation.ja.md)、障害時の管理は[運用ガイド](operations.ja.md)を参照してください。

別ホスト・低速実回線での連続運転、最大負荷と容量の実測、ディスク障害の網羅的注入、常駐Gateway更新後のDiscordコマンド操作一式は未実施です。今回の成功を、それらの環境受入まで完了した証拠にはしません。Proxyの復元やUNKNOWN管理解除を本番へ故意に起こす試験も行っていません。

初回v2検証時には稼働プロセスを再起動していません。現在の配備状況は末尾を参照してください。

## 通常投稿が継続不可として拒否される不具合の修正

開発中のDB更新で、Proxy会話未作成・過去依頼終了済みの場所まで一律に継続不可となっていた。新しい通常投稿の予約時に安全条件を確認し、同じDiscordの場所で自動初期化する。履歴と重複防止記録は保持し、旧依頼は再送しない。UNKNOWN・既存Proxy会話・明示停止は自動解除しない。

同じ場所への通常投稿だけで回答を取得する結合試験、過去依頼の保持・重複投稿・UNKNOWN・明示停止・既存Proxy会話の回帰試験を追加。通常テスト50件、Clippy、整形・差分チェック成功。

受付不具合の修正版は `cargo install --path . --locked` で `/home/tane/bin/codex-hoshikage-gateway` へインストール済み。既存configでローカルcheck成功。設定と稼働DBの手動変更・プロセスの停止は行っていない。

## 生成画像の自動添付（2026-09-12）

実装済み。Proxyの生成画像APIを照会し、本文とは独立してPNGを自動添付する。schema 5、永続監視、手動取得との重複抑止、明示再送、PNG検証、元依頼への返信参照、期限と復元照合を追加した。[仕様](generated-image-delivery-contract.ja.md)を参照。

実Codexで1枚生成→常駐Proxyの自動登録→Gatewayから許可済みDiscord先へ添付→Discord CDNから再取得してSHA-256一致とPNGデコード・画像内容を確認。774,344バイトの白地に青い円の画像。実送信は1回で、新たな生成依頼の再送はしていない。

画像自動添付実装時の確認（2026-09-13）: 通常テスト56件成功・失敗0件。実サービス専用5件は通常実行から除外し、今回追加した画像配信試験とBot添付権限の読取専用試験は明示実行で成功。Clippy（警告禁止）、整形、差分チェックも成功しました。

`cargo install --path . --locked` により修正版を `/home/tane/bin/codex-hoshikage-gateway` へ配置し、既存configによるローカルcheckに成功しました。設定変更は不要です。その後、末尾の記録どおりユーザー用systemdサービスへ反映しました。

## 画像進捗・モデル選択・状態照会の補正（2026-09-13）

画像の登録・配信待ちは同じ案内で進捗と結果を表示する。`/model` は選択済みでもメニューを開き、選択結果を次の依頼へ保存する。通常投稿形式でもメニューを表示し、ID指定や入力エラーの返答には古いメニューを付けない。

実運用記録では、送信開始直後にUNKNOWNとなり、約1秒後にRUNNINGへ戻る遷移が反復していた。送信中の通常照会競合を依頼ごとのガードで防ぎ、異常終了時にはガードを自動解放する。UNKNOWN等の案内が既に存在する場合は、正常状態の確認後は警告を削除する。正常復帰の追加投稿はしない。

受理を4秒遅延させてもUNKNOWNにならずPOSTが1回だけである結合試験、モデルメニュー選択とID指定・重複操作・本人以外の拒否、画像準備案内の更新を確認。通常テスト57件成功（全体実行後、変更したモデル試験を再実行して成功）。実サービス用5件は今回実行せず、AI生成やテスト投稿の再送はしていない。Clippyの警告禁止チェックも成功。

同日04:11 JSTに `~/bin` の修正版へユーザー用 `codex-hoshikage-gateway.service` を再起動し、active/running・起動ログにエラーなしを確認。既存configのcheck成功、設定変更なし。その後、利用者からモデル変更・画像生成の正常動作を確認済み。

正常状態に戻った後の「作業中であることを確認できました」等の投稿も廃止。所有確認済みの古い状態警告を削除し、削除履歴を保持する。再度問題が発生した場合には新しい警告を表示できる。配信・削除・再警告の回帰試験を追加した。

この補正の配信回帰試験4件・Clippy・整形・差分チェック成功。04:28 JSTに最終版をインストールしてユーザーサービスを再起動し、active/runningと既存の対象状態案内7件の削除確認・監査記録保持を確認した。

最終的な正常状態案内の抑制についても、利用者から実Discordで期待どおり動作したとの確認を得ました（2026-09-13）。

コミット前の最終全体確認: `cargo test --locked --tests --no-fail-fast --quiet` は58件成功・失敗0件、実サービス専用5件は除外。整形・差分チェックも成功。

## 承認UI・回答の配置・入力中表示（2026-09-15）

承認後に無回答に見えた事例を照合したところ、Discordの新規スレッド作成ではなく、承認カードより前の途中投稿へ確定回答を上書きしていた。承認内容も生JSONのままで、判断済みボタンが残っていた。

承認カードは目的・実行内容を整形し、専用タスクで回答済み・期限切れ等へ更新する。確定回答は末尾の別投稿にし、確認後に途中表示を削除する。利用者の選択により、独自の「思考中」投稿は作らずDiscord標準typing表示を作業中のみ継続する。

通常テスト61件成功・失敗0、実サービス専用5件は除外。承認を1回だけ送信して同じカードのボタンを除去すること、再クリックで二重承認しないこと、承認待ちと完了でtypingを止めること、最終回答が承認投稿より後に届いて途中表示だけ消えることを確認。Clippy・整形・差分チェック成功。新しい実AI依頼やDiscordへの試験投稿は行っていない。

10:51 JSTに修正版を `~/bin` へインストールし、既存設定のcheck成功後、Gatewayのユーザー用systemdサービスを再起動。active/runningを確認した。対象会話の既存承認6件がCLOSEDへ更新され、実Discordの既存カード1件を読み取り、整形された判断結果とボタン除去を確認した。設定変更・Proxy再起動は不要。新規依頼でのtypingの見え方と最終回答の配置は、模擬Discordで検証済みだが、実クライアントでの操作確認は未実施。
