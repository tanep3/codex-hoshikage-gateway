# 実装・検証状況

> **過去の開発記録です。** 以下のProxy接続・旧コマンド・当時の試験結果は、現在の公開版の操作説明ではありません。現行版はGatewayがCodex App Serverを直接起動します。導入は[導入手順](installation.ja.md)、操作は[ユーザーマニュアル](user-manual.ja.md)を参照してください。

更新: 2026-09-16
基準: 要件2.4、システム設計1.4、Proxy API v2契約案0.2およびMCP単一Run限定許可API合意0.3。

**生成画像の自動添付・モデル選択・状態案内の修正を実装し、ユーザー用systemdサービスへ反映済みです。利用者から実Discordでの正常動作の確認を得ました。** 以下は実装範囲と検証記録です。

## 実装済みの範囲

| 対象 | 内容 |
| --- | --- |
| 責務境界 | Gatewayにcwd登録を要求せず、Proxyが会話・ワーク・確定回答・成果物の正本を管理 |
| 会話と実行 | 独立会話作成、会話IDによる継続、元キーによる受理照会、最大1回送信、同一会話・workspaceの排他 |
| MCP対話 | mcp_form（flat-primitives-v1）の許可・拒否・本人専用入力、永続操作キーと照合復旧。実Discordの今回許可から正常動作まで利用者確認済み。拒否・追加入力の実環境受入は未確認 |
| 制御 | 実行枠とは独立した承認・Steer・停止・監視、Turn ID未確定時の停止、停止受付と完了の区別 |
| 選択UI | モデル・共有ワーク選択、フォーラムからの新規会話、成果物のページ送り・期限付き選択、共有範囲の明示選択 |
| 回答と成果物 | 固定ID取得、サイズ・SHA-256検証、強いETagとRangeによる中断取得再開、有限リースの取得・延長・解放照会 |
| 配信復旧 | Discord送信意思の永続化と受領照合、DB再オープン後の確定回答再取得、重複リスク確認付き /retry。AIを再実行して補わない |
| エラー処理 | 容量・期限・権限・破損の区別、取得のbackoff、未受理が明示されたcapture容量拒否だけ同一キーで再試行 |
| 表示とtemp | 通常成功時は回答本文を中心に表示、確定回答の余剰投稿整理、所有確認付き孤立キャッシュ回収 |
| DB・復元 | schema 6、既知schemaのtransaction移行とchecksum確認、旧依頼隔離、Proxy世代変更時の照合票と監査付き明示受入 |
| UNKNOWN | 自動再実行禁止、Proxy監査解除の照合、旧会話隔離、遅着RUNNING時の占有再取得 |
| 資料 | 英日README・導入・ユーザーマニュアル・運用手順を更新 |

v1の実行開始・監視処理は削除しました。モデル一覧と制御に使う既存API、旧DBを安全に移行する互換構造は残しています。

## 実サービスで確認したこと

常駐ProxyはAPI v2の必須Capabilityを満たし、成果物登録ツールが有効でした。生成画像対応の最終確認では、Gatewayの運用設定が契約2.0であることも確認しました。

1. **実Proxy＋実Codex＋模擬Discord**：専用会話を自動作成し、実モデルがテキストファイルを作成、専用ツールで成果物登録、確定回答を保存。Gatewayが一覧から選択し、同じ成果物を取得・検証して配信しました。
2. **Gateway DB再オープン**：回答のメモリを失った状態から同じResponseの確定回答を取得して配信。新たなAI実行は行っていません。
3. **実Discord**：許可された投稿先へ「Gateway v2 配信テスト」と上記成果物を送信。Discordで投稿IDと添付サイズを確認しました。運用中Gatewayを置換せず、隔離した試験DBから同じ配信コードを使用しました。

これらは tests/live_proxy_v2.rs の明示実行専用テストです。通常のテストでは実サービスへ送信しません。

## 自動試験

HTTP応答喪失、同一キー照会、世代不一致、初回失敗後の継続、停止競合、リース失効・延長応答喪失、部分ダウンロード、明示再送の重複操作、移行隔離、選択の期限・会話・世代照合、復元の明示受入、余剰表示削除を検証します。

v2基本実装時の確認結果（生成画像対応の最新結果は末尾）:

- `cargo test --locked --tests --no-fail-fast --quiet`: 50件成功、失敗0。実サービス用3件は通常実行では除外し、それぞれ明示実行で成功を確認。
- `cargo clippy --locked --all-targets -- -D warnings`: 成功。
- `cargo fmt --all -- --check` と `git diff --check`: 成功。
- 英日資料のローカルリンク: 存在確認済み。
- `cargo build --locked --release`: 成功。生成バイナリの `--version` 起動を確認。

## 配備と追加の環境試験

公開資料は未リリース製品の初回導入向けです。開発中のDB互換処理は実装上の保護であり、製品のV1→V2移行手順として案内しません。導入は[導入手順](installation.ja.md)、障害時の管理は[運用ガイド](operations.ja.md)を参照してください。

別ホスト・低速実回線での連続運転、最大負荷と容量の実測、ディスク障害の網羅的注入、常駐Gateway更新後のDiscordコマンド操作一式は未実施です。今回の成功を、それらの環境受入まで完了した証拠にはしません。Proxyの復元やUNKNOWN管理解除を本番へ故意に起こす試験も行っていません。

初回v2検証時には稼働プロセスを再起動していません。現在の配備状況は末尾を参照してください。

## 通常投稿が継続不可として拒否される不具合の修正

開発中のDB更新で、Proxy会話未作成・過去依頼終了済みの場所まで一律に継続不可となっていた。新しい通常投稿の予約時に安全条件を確認し、同じDiscordの場所で自動初期化する。履歴と重複防止記録は保持し、旧依頼は再送しない。UNKNOWN・既存Proxy会話・明示停止は自動解除しない。

同じ場所への通常投稿だけで回答を取得する結合試験、過去依頼の保持・重複投稿・UNKNOWN・明示停止・既存Proxy会話の回帰試験を追加。通常テスト50件、Clippy、整形・差分チェック成功。

受付不具合の修正版は `cargo install --path . --locked` で `/home/tane/bin/codex-hoshikage-gateway` へインストール済み。既存configでローカルcheck成功。設定と稼働DBの手動変更・プロセスの停止は行っていない。

## 生成画像の自動添付（2026-09-12）

実装済み。Proxyの生成画像APIを照会し、本文とは独立してPNGを自動添付する。schema 5、永続監視、手動取得との重複抑止、明示再送、PNG検証、元依頼への返信参照、期限と復元照合を追加した。[仕様](generated-image-delivery-contract.ja.md)を参照。

実Codexで1枚生成→常駐Proxyの自動登録→Gatewayから許可済みDiscord先へ添付→Discord CDNから再取得してSHA-256一致とPNGデコード・画像内容を確認。774,344バイトの白地に青い円の画像。実送信は1回で、新たな生成依頼の再送はしていない。

画像自動添付実装時の確認（2026-09-13）: 通常テスト56件成功・失敗0件。実サービス専用5件は通常実行から除外し、今回追加した画像配信試験とBot添付権限の読取専用試験は明示実行で成功。Clippy（警告禁止）、整形、差分チェックも成功しました。

`cargo install --path . --locked` により修正版を `/home/tane/bin/codex-hoshikage-gateway` へ配置し、既存configによるローカルcheckに成功しました。設定変更は不要です。その後、末尾の記録どおりユーザー用systemdサービスへ反映しました。

## 画像進捗・モデル選択・状態照会の補正（2026-09-13）

画像の登録・配信待ちは同じ案内で進捗と結果を表示する。`/model` は選択済みでもメニューを開き、選択結果を次の依頼へ保存する。通常投稿形式でもメニューを表示し、ID指定や入力エラーの返答には古いメニューを付けない。

実運用記録では、送信開始直後にUNKNOWNとなり、約1秒後にRUNNINGへ戻る遷移が反復していた。送信中の通常照会競合を依頼ごとのガードで防ぎ、異常終了時にはガードを自動解放する。UNKNOWN等の案内が既に存在する場合は、正常状態の確認後は警告を削除する。正常復帰の追加投稿はしない。

受理を4秒遅延させてもUNKNOWNにならずPOSTが1回だけである結合試験、モデルメニュー選択とID指定・重複操作・本人以外の拒否、画像準備案内の更新を確認。通常テスト57件成功（全体実行後、変更したモデル試験を再実行して成功）。実サービス用5件は今回実行せず、AI生成やテスト投稿の再送はしていない。Clippyの警告禁止チェックも成功。

同日04:11 JSTに `~/bin` の修正版へユーザー用 `codex-hoshikage-gateway.service` を再起動し、active/running・起動ログにエラーなしを確認。既存configのcheck成功、設定変更なし。その後、利用者からモデル変更・画像生成の正常動作を確認済み。

正常状態に戻った後の「作業中であることを確認できました」等の投稿も廃止。所有確認済みの古い状態警告を削除し、削除履歴を保持する。再度問題が発生した場合には新しい警告を表示できる。配信・削除・再警告の回帰試験を追加した。

この補正の配信回帰試験4件・Clippy・整形・差分チェック成功。04:28 JSTに最終版をインストールしてユーザーサービスを再起動し、active/runningと既存の対象状態案内7件の削除確認・監査記録保持を確認した。

最終的な正常状態案内の抑制についても、利用者から実Discordで期待どおり動作したとの確認を得ました（2026-09-13）。

コミット前の最終全体確認: `cargo test --locked --tests --no-fail-fast --quiet` は58件成功・失敗0件、実サービス専用5件は除外。整形・差分チェックも成功。

## 承認UI・回答の配置・入力中表示（2026-09-15）

承認後に無回答に見えた事例を照合したところ、Discordの新規スレッド作成ではなく、承認カードより前の途中投稿へ確定回答を上書きしていた。承認内容も生JSONのままで、判断済みボタンが残っていた。

承認カードは目的・実行内容を整形し、専用タスクで回答済み・期限切れ等へ更新する。確定回答は末尾の別投稿にし、確認後に途中表示を削除する。利用者の選択により、独自の「思考中」投稿は作らずDiscord標準typing表示を作業中のみ継続する。

通常テスト61件成功・失敗0、実サービス専用5件は除外。承認を1回だけ送信して同じカードのボタンを除去すること、再クリックで二重承認しないこと、承認待ちと完了でtypingを止めること、最終回答が承認投稿より後に届いて途中表示だけ消えることを確認。Clippy・整形・差分チェック成功。新しい実AI依頼やDiscordへの試験投稿は行っていない。

10:51 JSTに修正版を `~/bin` へインストールし、既存設定のcheck成功後、Gatewayのユーザー用systemdサービスを再起動。active/runningを確認した。対象会話の既存承認6件がCLOSEDへ更新され、実Discordの既存カード1件を読み取り、整形された判断結果とボタン除去を確認した。設定変更・Proxy再起動は不要。新規依頼でのtypingの見え方と最終回答の配置は、模擬Discordで検証済みだが、実クライアントでの操作確認は未実施。

### 承認後の本人専用メッセージの重複解消（2026-09-15）

利用者が指していたのは新規スレッドではなく、承認後のephemeral確認メッセージだった。通常コマンドと同じtype 5の受付・返信を使っていたことが原因。承認ボタンのみtype 6で受け付け、正常時は既存の承認UIタスクによる元カード更新だけにする。エラー・タイムアウト時には本人専用のエラー通知を残す。通常コマンドの返信方法は維持する。

承認UI試験2件成功。実際のcontrol loopを使い、type 6受付、正常時の追加返信なし、二重承認なし、失敗時の本人専用エラーを確認。Clippyの全ターゲット警告禁止チェック・整形・差分チェックも成功。実Discordでの新しいボタン操作は未確認。

11:06 JSTに `~/bin` へインストールし、既存設定check成功後にGatewayのユーザーサービスを再起動。active/runningを確認。設定・Proxyは変更していない。


## MCP承認・入力と停止理由（2026-09-16）

Proxyの `gateway-mcp-approval-change-request.ja.md` と対話API追加契約0.1に対応。常駐Proxyへの読み取りで `interaction_relay=true`、flat-primitives-v1、最大16件・65536 bytes・期限600秒を確認した。障害対象2 Responseのerror.codeはunsupported_interaction、interaction_capabilitiesは空配列だった。

Gatewayは検証済みのmcp_formだけを実行要求に宣言する。サーバー名・確認文面を表示し、空フォームは今回許可/拒否、非空フォームは本人専用の項目選択・modal・明示送信で扱う。32項目、64候補のenum、string/integer/number/boolean、required・上下限・Unicode文字数を検証する。8192 bytesの文字列は複数欄で入力できる。回答をDB・公開チャットへ保存しない。未送信の入力はBot再起動後に再入力する。

schema 6は要求ID・revision・期限・Proxy世代・操作キー・digestを保持する。独立監視と元カード更新で復旧し、回答POSTは最大1回。通信断ではoperationとinteractionのGET照合だけを行う。追加質問を通常のコマンド承認へ変換しない。mcp_url/user_input/permissionsは未宣言。未対応による停止、期限切れ、対話接続喪失、利用者停止・確認できた拒否を区別する。

全体試験71件成功・失敗0件、実サービス専用5件は除外。MCP追加試験10件は二段階の承認→入力、本人限定、二重クリック、停止・失効・世代変更、送信応答喪失、再起動後の照合、長文・ページ分割・秘密の非永続化、実行要求の宣言、終了理由保存、schema 5からの移行を含む。Clippy（全ターゲット・警告禁止）、整形、差分チェック成功。

**実Discordでの新しい承認操作と実MCP成功の結合受入は未確認。** 利用者と「既存チャットでYahoo! JAPANを開く→今回許可→見出しの回答」を実施し、ProxyのMCP実行結果を照合する必要がある。拒否経路も別途確認する。モック成功やMCP接続確認だけを実Discord受入完了とは扱わない。

反映前に稼働中Gatewayの正式なadmin backup経路で `/tmp/gateway-before-mcp-20260916` を作成し、schema 5・integrity okを確認済み。

2026-09-16 06:46 JST、修正版を `~/bin` にインストールし、既存設定check成功後にユーザー用Gatewayサービスを再起動した。schema 6への移行、active/running、NRestarts=0、Discord connected=true、Proxy ready=true、復旧保留なしを確認。Proxy設定・サービスは変更していない。今回の実Discord承認・実MCP結合受入は上記のとおり未確認。

### 実Discord承認の受信変換修正（2026-09-16）

利用者試験で「今回許可」は表示されたが、押すとDiscordの応答期限切れになった。Serenity 0.12.5がInteractionをJSONへ戻す際にwireのtypeを含めない一方、MCP操作はtype=3/5を検証していたことが原因。送信意思・操作キーは未作成で、Gatewayから承認は送っていなかった。

受信境界で列挙型のkindからtypeを明示復元した。SerenityによるDeserialize→Serialize→実control loopの回帰試験で、MCP承認がProxyへの回答1回につながり、追加の本人専用成功投稿がないことを確認。modalのtype・入力値保持も確認。MCP試験11件成功。旧モックがこの受信変換を省いていた点が検証漏れだった。

MCP非対応だった過去の全依頼を反復照会する処理も修正。schema 6導入以降またはMCP記録がある依頼に絞り、失敗時は30秒の再照会間隔を設け、ログに要求IDを付けた。

この補正はMCP11件・通常承認2件・モデル操作1件・制御3件の計17件、Clippy・整形・差分チェックに成功。06:56 JSTにインストール・Gateway再起動しactive/runningを確認した。Proxyを再起動せず、元のMCP要求がpending/not_sentのまま、期限06:58:13で残っていることをGETで確認。承認・元依頼の自動再送はしていない。利用者の再操作による実Discord受入は未確認。


2026-09-16、上記受信変換の修正版について、利用者から実Discordで「うまくいった」と確認を得た。今回許可ボタンから作業が継続する通常経路の利用者確認済み。拒否・追加フォームの実環境受入、およびProxyのTurn内MCP結果との独立照合まで完了したという意味ではない。先の未確認表記はその時点の検証記録として残す。

## 2026-09-16 配信エラー案内

- 保存版取得待ちとDiscord送信結果不明を分け、待機・会話内の確認・明示再送・運用者への依頼を案内する。期限切れ回答に /get を勧めない。
- 初回エラー文を固定表示せず、配信記録から案内を更新する。配信完了・明示再送への切替後は所有確認済みの警告を除去する。AI再実行や結果不明送信の自動再送は追加しない。
- resource_notices 3件、delivery 6件が成功。Clippy全target（警告エラー化）、fmt、差分チェック成功。
- 常駐ユーザーサービスへ反映。既存警告をDiscord APIで読み戻し、送信結果不明・/retry・重複の説明への更新を確認。利用者のクライアント画面での見え方と明示再送操作は未確認。

## 2026-09-16 ファイル変更の承認形式

- 実Proxyの取消済み承認を読み取り、対象が `changes` ではなく `paths: string[]` で届いていたことを確認。Gatewayの表示・承認可否判定を両形式に対応させた。空・不正な対象、未解釈の追加許可範囲は承認不可のまま。
- ボタンがない場合に「今回のみ承認」の説明を表示しない。取消と運用者への連絡を案内する。
- 実応答形式を使った承認UI・押下のMock結合試験を含む3件、Clippy全target、fmt、差分チェック成功。実Discordでの次回のファイル承認は未確認。
- 修正版バイナリをインストールし、Gatewayのユーザーsystemdサービスを再起動。active/running・NRestarts=0を確認。取消済み承認の再実行・自動承認は行っていない。

## 2026-09-16 MCPターン限定許可：方針反映・表示の先行実装

**今回の変更は未配備。常駐環境の自動許可は有効にしていない。**

- 要件・設計・調整案に双方の合意を反映：1依頼/1 Run、並列Run分離、Steerで失効と依頼内容世代更新、危険ツール除外、browser_evaluate実証、操作内容表示。
- 既存APIで実装：確認原文と実引数照合の不足を明記、今回だけ許可のボタン、許可/フォーム回答の送信確認済み・resolvedのカードを1依頼の件数表示へ集約。操作本文や回答をSQLiteへ追加保存せず、個別の配信監査記録を残す。
- 個別カードはボタン無効化・要約の送信確認・Bot所有確認後に削除。削除中断/再起動時に再作成しない。拒否・期限切れ・不明、利用者回答の証拠がないresolvedは成功件数に含めない。
- 未実装の理由：Proxyの具体API契約と上流call対応の実証結果がまだない。許可作成/取消・同一性照合・実引数取得・Steer失効は推測で実装しない。新しい自動許可Capabilityも宣言しない。
- 受入の残り：具体API接続とDB設計、権限範囲と危険ツール判定、並列/停止/Steer/再起動、実MCP/実Discord。ユーザーには契約書のパスを照会済み。
- 検証：MCP 16件、通常承認UI 3件、配信 6件が成功。Clippy全target（警告エラー化）、fmt、差分チェック成功。実送信・常駐反映は今回実施していない。

## 2026-09-16 API待ちの間の復旧強化

- 先行実装の自己レビューで、古いPOST/PATCHの照合が最新カード内容の配信成功として返る不具合を回帰試験で再現し、Deliveryを修正。古い版を照合した場合は最新表示の成功とせず、次の処理周期で更新する。
- 結果不明から解消後のカード整理中、作業終了や再起動でclosedが早期確定しないよう修正。確認済みresolvedは終了時の監視でも整理を継続する。
- 別依頼の回答を集約件数に混ぜず、別カードを削除しない試験を追加。
- [API接続準備・受入表](mcp-turn-approval-acceptance.ja.md)を追加。APIパス・型や自動許可は仮実装していない。常駐への反映は行っていない。
- 検証：全体の回帰テスト85件成功・失敗0件・実環境用5件はignored。MCP関連18件、配信関連7件を含む。Clippy全target（警告エラー化）、fmt、差分チェック成功。

## Proxy MCPターン許可API契約案0.1のレビュー

具体契約の初稿を受領し、要件・設計へ本人限定表示とAPI責務を反映。[接続レビュー](mcp-turn-approval-api-review.ja.md)にCapability/応答型・user_inputフォールバック・表示内容の照合・既存Steer経路・browser_evaluate代替実証の確認事項を整理した。Proxyは実装・隔離実証中。Gatewayの新API接続・常駐自動許可はまだ未実装/無効。本回は文書のみ変更し、テスト・サービス反映は実施していない。

## Proxy MCP API契約0.3の照合

Gateway接続レビュー5項目は解消し、契約0.3を実装基準として受け入れた。[照合結果](mcp-turn-approval-api-review.ja.md)へ記録。関連要件・設計・interaction契約追補・開発工程との整合も確認した。今回は文書のみ更新し、コード・設定・サービスは変更していない。

Gateway側の次工程は、本人限定画面・固定approval_context・許可/取消の永続化・エンドポイント別上限を詳細設計/DB/受入へ反映してから接続実装。Proxyの実装とA01〜A11の受入は未完了であり、常駐自動許可は未有効化。0.1時点の「具体契約待ち」は履歴として残すが、現在の接続契約は0.3で確定したものとして扱う。

## 2026-09-16 合意API 0.3のGateway接続実装

**Gateway実装・Mock受入済み。常駐へのインストール、サービス再起動、Proxyの自動許可有効化は未実施。** 要件2.4・設計1.4と [詳細設計](mcp-turn-approval-gateway-design.ja.md) を先に確定して実装した。

- 対応Capabilityを独立検証し、新規依頼に保存済みの不透明principal/channelとrun_idを付ける。旧Proxyと内部入力フォームの既存経路を維持する。
- 操作詳細を本人限定のページへ表示。原文コードの記号を維持し、秘匿と取得不能を区別する。公開カード・DBへ実引数を保存しない。許可範囲の説明とボタンを同じ応答で表示する。
- 単発とターン限定を別の明示操作にし、表示revision/fingerprint・全scopeを再照合。古い個別ボタンから詳細確認を迂回できない。browser_evaluate/browser_run_code_unsafeのターン許可は不可。
- /mcpと依頼固定のカードから一覧・取消。取消意思と操作キーを永続化し、応答喪失後は一覧再表示で元operationを照会する。取消済みでも送信中/結果不明件数を確認できる。
- schema 7へ前進移行。interaction/operation、grant一覧、interaction一覧の応答上限を個別化し、大きな一覧の取得・処理を直列化。英日マニュアルを更新した。

検証結果：
- 全体92件成功、失敗0、実サービス用5件はignored。
- 最終表示変更後、ライブラリ4件とMCP結合24件を再実行し成功。
- Mockで本人限定表示、同時押下と応答喪失時の最大1回送信、表示後のfingerprint/各scope変更拒否、危険ツール、固定context送信、取消応答喪失とApp再作成後の照会、schema 6→7の既存対話保持を検証。
- schema 1/2の移行試験も新テーブルを含めて旧DBの再現を更新し成功。
- Clippy全target（警告禁止）、fmt、git diff --check成功。

未完了：実Proxy/App Serverのcall対応・並列Run/Steer/停止/設定世代の競合受入、実ブラウザの代替経路による確認削減、実Discordでの表示・ボタン操作、常駐配備。browser_evaluateの連続確認削減を達成済みとは扱わない。コミット・pushは今回実施していない。

### Gateway常駐反映（2026-09-16 18:49 JST）

利用者の指示で `cargo install --path . --locked` を実行し、既定の `~/bin/codex-hoshikage-gateway` を更新。既存configのcheck成功後、ユーザー用 `codex-hoshikage-gateway.service` を再起動した。設定ファイルとProxyの有効化設定は変更していない。

切替前の実行中依頼・holdは0。正式なadmin backupで `~/.config/codex-hoshikage-gateway/backup-before-mcp-run-20260916` にschema 6の整合したバックアップを保存した。再起動後はschema 7とmigration記録、active/running・NRestarts=0、Discord connected=true、Proxy ready=true、recovery_pending=falseを確認。配置バイナリと実プロセスのSHA-256一致、今回の起動ログにエラーなしも確認した。

Gatewayの配備は完了。新MCP APIの接続受入・ターン許可の実操作試験はProxy側の準備後に実施する。既存Proxy readyの確認を、新機能の受入成功とは扱わない。Discordへの試験投稿・新規AI依頼は今回実施していない。


## 2026-09-16 合意インラインAPI 0.4のGateway実装

**Gateway実装とMockを含む回帰検証は完了。今回の常駐反映・実Proxy 0.4接続・実Discordでの押下確認は未実施。** 要件2.5・システム設計1.5・詳細設計を先に更新し、0.4 R-01対応版を基準に実装、その後に英日マニュアルを更新した。

- 最初のカードにProxyの公開用操作・対象と「この依頼中、このツールを許可」「今回だけ許可」「拒否」をまとめる。ターン対象外は2択。許可一覧・取消は `/mcp` に分離。
- 秘匿・未評価操作・長文では補足確認へ切り替える。表示不正・取得不能では許可ボタンを出さない。本人限定の生引数から公開説明を生成しない。
- schema 8へ宣言・表示版を保存。新規Responseだけに公開表示方式を宣言し、旧依頼へ後付けしない。本文を保存せず、元会話・message ID・確定した本文/ボタンdigest・表示トークンを照合する。
- 単発・ターン返信に表示トークンを付け、既存の最大1回送信・結果照会を共用。再起動、応答喪失、古いカード・別利用者・別会話の拒否を検証。
- 英日導入手順には検索語・URLの公開範囲と利用者への方針確認、既存Proxyスイッチと追加Capability確認を追記。英日利用者マニュアルは新カードの操作と補足確認の例外を説明。

最終検証：`cargo test -j 1 --no-fail-fast` は101件成功・失敗0・実環境用6件ignored。MCP結合33件（今回追加9件）を含む。旧schema再現用fixtureに8のテーブル削除を加え、画像配信の既存テストは通知更新workerの終了を待って最終表示を検証するよう同期を修正した。Clippy全target（警告禁止）成功、最後のテスト同期修正後も当該targetのClippy成功。fmt・diffチェック成功。

実Proxy/Discordの残るGateは [受入記録](mcp-turn-approval-acceptance.ja.md) に記載。0.3で実証済みの結果を0.4の実証へ流用しない。設定・サービス・本番ツールの確認モードは変更していない。今回コミット・pushは実施していない。


## 2026-09-17 インライン0.4の常駐反映・接続確認

利用者の依頼により `cargo install --path . --locked -j 1` で `~/bin/codex-hoshikage-gateway` を更新。本番の依頼が終了したことを確認して、04:59 JSTにユーザーサービスを再起動した。直前の正式バックアップは `~/.config/codex-hoshikage-gateway/backup-before-inline-20260917-final`（schema 7、integrity=ok）。

再起動後はschema 8／migration記録、active/running、NRestarts=0、Discord connected=true、Proxy ready=true、recovery_pending=false、holdなしを確認。配置ファイルと実プロセスのSHA-256はともに `58e80f7fcbf86b004bfadc2d87744240394e9d87ca1dd02a676eff2571781f59`。

実Proxyはbc28861配備済みのバイナリを使用。常駐の3 capabilityは有効。隔離した同一バイナリ・実Codex・実MCPとGatewayで、新カードから依頼中許可→5回適用→失効→次依頼では単発5回の接続試験が成功。[受入記録](mcp-turn-approval-acceptance.ja.md)にIDと保証範囲を記載。Discord HTTPは試験用であり、本人による実Discord表示・押下の受入は別途必要。

切替前に利用者が確認したbrowser_tabs/listの二段階画面は旧Gatewayで開始した依頼だった。切替後の新規Responseから公開カードを宣言する。旧Responseへ方式を後付けしない。本番Proxyのターン許可対象はplaywright.browser_findのみで、browser_tabsは公開カードでも単発／拒否の2択。今回Proxyの設定・権限範囲・本番の常時許可設定は変更していない。


取消接続試験も完了：新カードで付与→Gatewayから取消→残り4回を個別確認→5回の検索完了、operator_revokedと適用件数を照合した。取消後の1回は本人限定補足経路だったため、全操作が公開カードで完結したとはしない。試験用Discordと操作選択のハーネスを修正し、Clippy（live_mcp_gateway target）・fmt・差分チェック成功。通常回帰101件は前工程の成功結果を維持し、今回のアプリ本体コード変更はない。配備後の変更は試験と記録のみ。
