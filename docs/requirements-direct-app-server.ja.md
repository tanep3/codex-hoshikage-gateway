# Gateway専属Codex App Server — 目標要件

> **現在は直接接続版を実装済みです。** 本文の「目標」「未確定」「切替前」は設計時点の記録です。現在の導入と操作は[導入手順](installation.ja.md)と[ユーザーマニュアル](user-manual.ja.md)を参照してください。

版0.2 / 2026-09-18 / Tane Channel Technology

状態：目標構成の要件案。[構造転換の決定](architecture-direction-direct-app-server.ja.md)に基づく。現行サービスと[現行要件](requirements.ja.md)の動作を直ちに変更しない。移行手順と未確定の製品選択は本書末尾に残す。

## 目的と製品境界

Discord上の会話、作業依頼、承認、成果物受取を、一つのGateway製品として提供する。Gatewayが専属Codex App Serverを子プロセスとして起動し、stdioの双方向通信で操作する。稼働時にcodex-hoshikage-proxyのHTTP APIやサービスを必要としない。Proxyは別製品として残す。

**プロセス境界をなくしても内部の責務は分ける。** Discordの表示変更がCodex通信の解析を変えず、App Serverの通知変更がDiscord認可を変えないようにする。内部の境界はRustのモジュール、型、呼出し方向、保存上の正本で表す。汎用HTTP APIをGateway内に再構築することは目的ではない。

## 必須要件

| ID | 要件 |
| --- | --- |
| DA-R01 | Gatewayだけが専属App Serverを起動・監視・終了させる。同じ子プロセスをProxyや他アプリと共有しない |
| DA-R02 | 子プロセスのstdin/stdoutを双方向JSON-RPCとして扱い、要求・通知・App Server起点の承認／入力要求を欠落・取り違えなく処理する。Codexの実引数と承認対象を忠実に表示できる情報を保持する |
| DA-R03 | Codex通信層はDiscordのGuild、チャンネル、メッセージ、Botトークンを知らない。Discord層はJSON-RPCを直接書き込まず、Gateway内部の実行・承認操作を呼ぶ |
| DA-R04 | 依頼の受付、最大2並列・同一会話1実行のキュー、モデル選択、会話継続、Steer、停止、取消、進捗、追加質問を維持する。操作の意味は現行UIと一致させる |
| DA-R05 | Discord Message IDと依頼、Codex thread／turn、実callを永続的に対応付ける。受付重複、遅延通知、別会話の結果、古い承認画面を取り違えない |
| DA-R06 | App Server送信前に送信意思と対象を保存する。送信結果不明のTurnや承認を新しいIDで自動再送しない。停止の受付と完了を区別する |
| DA-R07 | App Serverの終了、stdio切断、通知欠落、Gateway再起動時に、確認可能な状態を照合する。確認不能な実行はUNKNOWNとして保護し、自動実行し直さない |
| DA-R08 | Codex認証、実行先、サンドボックス、ネットワーク、承認方針はGateway専属のCodex設定・運用権限で管理する。Discordから設定を勝手に拡張しない |
| DA-R09 | 最終回答、生成画像、成果物の復旧可能な保存版をGateway自身が管理する。回答・保存・Discord配信の状態を分け、配信失敗でAIを再実行しない |
| DA-R10 | 承認は実callと完全な引数、現在の依頼・入力世代に結び付け、表示内容と押下内容を一致させる。拒否は詳細取得不能でも使える。意味ベースの委任は後続の独立要件として再設計し、今回の直接接続だけで自動有効化しない |
| DA-R11 | 生成画像と成果物の発見・保存・配信の現行利用者動作を維持する。ファイル名やモデルの説明文だけで別会話のファイルを公開しない |
| DA-R12 | 本人・Guild・会話の認可、Discord添付・メッセージの容量と結果不明時の照合、利用者に次の操作が分かるエラー案内を維持する |
| DA-R13 | Gatewayの設定正本は `~/.config/codex-hoshikage-gateway/config.toml`。Codex起動、専属設定、保存先、権限・上限をここで管理し、Proxyの設定へ依存しない。秘密値は別ファイルで保護する |
| DA-R14 | バックアップ、復元隔離、schema migration、監査、管理解除、systemd user serviceの保護を継続する。DBとファイル保存物を一貫した単位で扱う |
| DA-R15 | 旧Proxy経由の依頼・回答・成果物を、切替後に新方式の実行済み記録と誤認しない。未完了や結果不明を自動再送しない。旧データの参照可能期間と切替手順を明示する |
| DA-R16 | App Serverが生存したまま応答しない場合も、初期化・Turn・承認返信・通知監視それぞれの待機を有限にする。送信境界後に到達・適用を確定できないTurnや承認は結果不明として保護し、自動再送しない。初期化前の不調を実行済みTurnのUNKNOWNと混同しない |
| DA-R17 | Gatewayの正常停止、異常終了、systemd再起動で古いApp Serverを残して新しい子プロセスを起動しない。残存の有無を検証できない場合は新規実行を開始しない |
| DA-R18 | 全体2並列を維持し、各RunをGateway所有の専属App Server子プロセスで実行する。承認・停止はそのRunの子プロセスへ届け、他Runの設定や要求に流用しない |
| DA-R19 | `/model` は現在値の表示と、Codexが返したモデル候補からの選択を提供する。候補を押した時点で会話と本人を再確認し、選択値を再検証してから次の依頼へ反映する。候補数がDiscordの表示上限を超えても、残りを選べないまま黙って切り捨てない |
| DA-R20 | 会話ワークの親ディレクトリをGateway設定で指定できる。変更前に開始した会話の実行先・ファイル・Codex文脈は維持し、新規会話から新しい親を使う。利用者はDiscordから現在の会話ワークの場所を確認できる。旧ワークを設定変更だけで移動・削除しない |
| DA-R21 | Codexが `mcpServer/elicitation/request` の空フォームでMCPツールの実行確認を求めた場合、Gatewayは本人限定でサーバー名・操作文面・実引数を表示し、完全に確認できた1件に限り「今回だけ許可」と「拒否」を提供する。`persist` の候補があっても包括許可へ昇格しない。形式不明・内容欠落・表示不能は許可せず、拒否と中断を明確に案内する |
| DA-R22 | 承認ボタンは上流が提示した判断とGatewayが実際に返せる応答だけを表示する。`decline` が返せない未対応形式の終了を「拒否」と呼ばず、利用者には形式未対応の結果を伝える |
| DA-R23 | 未知のApp Server確認要求は、該当RPC要求IDへ形式未対応エラーを返して待機を解消する。上流が承認要求の解決を通知したらGatewayの承認操作を閉じ、古いボタンから再返信しない。Discord画面の更新失敗はAI実行の失敗と混同しない |
| DA-R24 | 構造化されたMCP実callのサーバー名・ツール名・引数を上流の確認要求に一意に照合でき、上流がセッション許可を提示し、かつ個別確認必須ツールでない場合、本人に「この依頼中、このツールを許可」を単発許可と別の明示選択として提示する。選択後は同じRun・入力世代・MCPサーバー・ツールだけを後続自動承認の候補にする。引数が変わる呼出しも対象になることを明示する。証拠欠落・重複・設定変更・Steer・停止・Run終端では自動承認を適用しない。次のDiscord投稿や会話の別Runへ引き継がない |
| DA-R25 | コマンド・ファイル変更・MCP確認の実引数がDiscordの1画面に収まらない場合、本人限定画面で全文を欠落なく分割表示する。ページ番号と前後移動を示し、すべてのページを順に表示するまで許可ボタンを有効にしない。上流が提供する拒否または取消と、作業の中断は途中でも利用できる。上流の受理上限を超える入力や表示不能は許可せず、理由と次の操作を案内する |
| DA-R26 | 再起動後のUNKNOWNが同一会話の待機依頼を塞ぐ場合、受付時に「未送信のまま保留」と理由・次の操作を本人へ知らせる。旧依頼を自動再送しない。運用者は停止したGatewayの整合バックアップを作り、対象ID・状態・占有世代を照合した監査付き管理操作で占有だけを解除し、その会話のCodex文脈を新規に切り替えられる。UNKNOWN自体は消去・成功化しない |
| DA-R28 | UNKNOWNで会話が進められない場合、許可された本人はDiscordの `/recover` から影響を確認し、明示的に同じ会話を復旧できる。Gatewayは復旧前に整合バックアップを作り、対象のUNKNOWN・占有世代・実行中の有無を再検証する。旧依頼を再実行せず、未送信の待機依頼を取消記録に残し、Codex文脈だけ新規にして待機列を再開する。Discord履歴・ワークファイル・旧UNKNOWN記録は保持する。復元隔離中、実行中、対象不一致、バックアップ失敗時は解除しない |
| DA-R27 | Run actorが終了して制御チャンネルが閉じた後でも、`/stop` は会話の待機列を停止できる。Codexへの中断を確認できない場合は「待機列の停止」と「実行停止の未確認」を別々に示し、操作エラーで無回答にしない。`/cancel` は未送信の待機依頼を優先して取り消す。結果不明の依頼を推測で完了・取消へ変更しない |

## 内部責務の不変条件

- Codex transportはプロセスとプロトコルを所有し、利用者の権限やDiscordへの表示を決定しない。
- 実行管理は会話、依頼、Turn、作業先、モデル、排他、停止を所有し、Discord APIを直接呼ばない。
- 承認管理は上流の実call、表示証跡、明示判断、送信状態、失効を所有する。意味判定を後で追加してもCodex transportに埋め込まない。
- 保存管理は確定回答と不変成果物、保持期限、ハッシュ、取得状態を所有し、Discordへの送信先を決定しない。
- Discord層は本人確認、画面、配信を所有する。Codexや保存の結果を推測で成功に書き換えない。
- SQLiteの一つのレコードだけで「AI実行・回答保存・Discord配信」の三つを完了とみなさない。

## 受入条件

実行手順と判定は[直接接続版の切替・受入テスト](direct-app-server-cutover-test-plan.ja.md)に定める。内部試験と利用者が見るDiscord画面の両方を確認してから常駐切替する。

| ID | 検証 |
| --- | --- |
| DA-A01 | Proxyサービス停止中でも、専属App Serverで新規依頼・継続・モデル選択・承認・停止・成果物返送が動く |
| DA-A02 | Discordの別人／別会話の入力と古い承認が、実callや結果へ混ざらない |
| DA-A03 | 送信直前・送信直後・応答喪失・子プロセス異常終了・Gateway再起動で二重実行／二重承認を起こさず、UNKNOWNを保持できる |
| DA-A04 | App Server起点の承認・MCP入力と通常通知が並行しても、実call・引数・Turnの対応が維持される |
| DA-A05 | 最終回答・画像だけの回答・複数成果物・Discord送信失敗から、同じ保存版で復旧できる。期限切れや破損を成功表示しない |
| DA-A06 | 実行枠が満杯でも承認・拒否・停止・状態確認が進む |
| DA-A07 | 実Codexと実Discordで主要動線を確認し、模擬App Server成功だけを製品受入としない |
| DA-A08 | 旧Proxy経由の未終端依頼を、切替時に新しいCodex Turnとして自動開始しない。移行・保留・参照不可の状態を本人に説明できる |
| DA-A09 | 子プロセスは生存したままinitialize応答、Turn応答、承認返信応答、stdoutの読み取りが止まるケースを注入する。初期化時はreadyにせず、送信済みTurn／承認は到達を推測せず照会またはUNKNOWNで保護し、自動再実行・自動再許可しない |
| DA-A10 | Gatewayの正常終了・SIGKILL相当の異常終了・systemd再起動を隔離環境で試し、旧App ServerのPIDとその子孫が残らず、新旧の実行が重ならないことを確認する。回収を確認できない場合は新規受付を止める |
| DA-A11 | transport単体試験の直後に、同一App Serverの2並列Turnと別子プロセスからのthread再開を実証する。その結果を踏まえてRun別子プロセスを採用し、2並列RunのMCP設定、承認、停止、通知、モデル、出力の混線がないことを後続の結合試験で確認する |
| DA-A12 | `/model` の選択メニューから候補を変更し、`/model` と次の回答が同じ選択値を使う。別人・別会話・古い選択の取り違えがなく、候補上限を超える場合も操作方法を案内する |
| DA-A13 | ワーク親ディレクトリを設定変更しても既存会話の `report.txt` と継続実行は元の実行先で使え、新規会話は新しい親に作られる。Discord上で実際の保存先を確認できる |
| DA-A14 | 実コードを含むMCPツール確認の空フォームを再現し、本人限定画面に実引数と「今回だけ許可」「拒否」が出る。許可は同じRunの同じRPC要求へ `accept` と空の `content` を1回だけ返し、拒否はJSON-RPCエラーで代用せず `decline` を返す。古い画面・別Run・不完全な表示からは許可しない |
| DA-A15 | 未知の確認形式を注入しても同じRPC要求へ形式未対応を返し、Turnを無期限に止めない。解決通知後は承認カードのボタンが消え、古い画面を押しても再返信しない |
| DA-A16 | 同じRunで同じMCPツールを2回使うと、明示した依頼中許可後の2件目は再確認なしで1回だけ実行する。別ツール・別Run・Steer後・停止後・対応付け不能・個別確認必須ツールでは自動許可しない。クラッシュ後に古い許可を復元しない。単発許可は依頼中許可へ昇格しない |
| DA-A17 | 1900文字を超える実コマンドを複数ページで欠落なく本人限定表示し、最後のページ以前の許可を拒む。ページ途中でも上流が提示した拒否または取消を使える。ページ操作の重複、古いカード、別会話、上流解決後から許可されず、短いコマンドの1ページ表示も維持する |
| DA-A18 | 承認待ちでサービスを停止・再起動し、旧依頼がUNKNOWNになった同一会話へ投稿する。新しい依頼は未送信のまま保留され、理由を通知する。停止中の管理解除はバックアップ・対象ID・占有世代・明示的なリスク受容を必須とし、旧UNKNOWNを維持したまま新しいCodex文脈で待機依頼を開始できる |
| DA-A19 | 本人が `/recover` を開くと、文脈の新規開始と未送信依頼の取消件数が本人限定で表示される。確認前・他人・別会話・古い占有世代からは解除できない。確認後はバックアップと監査記録を残し、旧UNKNOWNを再実行せず、同じDiscord会話の次の新規投稿が新しいCodex文脈で進む。バックアップ失敗・復元隔離・実行中には解除されない |

## 未確定事項と推奨

1. **専属子プロセスの個数（決定済み）**：同時最大2つ、Runごとに専属子プロセスを所有する。単一子プロセスの2並列Turnと、別子プロセスからのthread再開は実機で確認した。ただし単一子プロセスにおけるMCP設定・承認方針の完全な隔離は未実証なので採用条件にしない。Run別構成の停止・承認・異常終了・復旧は引き続き受入試験を行う。
2. **既存会話の切替方針（確定）**：Discordの過去投稿と旧Gatewayの実行・配信記録は保持する。切替後の最初の発言から新しいCodex文脈を始める。旧Proxyのthread／response IDを新しいCodex子プロセスへ引き継がず、旧依頼を再送しない。切替直前の整合したバックアップを作成・検証する。同じDBを直接方式へ変換する場合は未完了の依頼・配信があれば拒否する。未完了の配信を確認できない場合は、旧DB・保存物を検証済みの参照用アーカイブとして保持し、新しいstate_dirに直接方式のDBを初期化する経路を認める。旧レコードは完了・成功へ書き換えず、新Gatewayから再送しない。アーカイブの所在と未確定件数を記録し、後で照合できるようにする。旧保存物の閲覧機能と保持期間は別途確定する。
3. **Codex専属設定の置き場**：設定ファイルの正本はGateway配下。認証情報とセッション保存先はファイル権限、旧Proxyとの同時利用、バックアップ範囲を確認して確定する。運用者の既存ログイン状態を黙ってコピーしない。
4. **画像・成果物の保存期間と容量**：現在の製品の利用実態とディスク容量から決める。Proxyの数値を根拠なくそのまま写さない。
5. **モデルの識別と互換表示**：既存の利用者向けモデル名を継続できるかをApp Serverからの情報で確認する。変更が必要なら利用者へ明示する。

本書は目標機能を定める。現在の設定例・導入手順は切替まで現行Proxy経由の運用説明として維持し、新方式の実装・受入後に英日双方を改定する。
